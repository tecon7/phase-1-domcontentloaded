// Your code goes here

function updateContent(message) {
let p = document.addEventListener("DOMContentLoaded", function(event) {
  const p = document.querySelector('p'); 
  p.textContent = message;
  });
}
updateContent('This is really cool!')
